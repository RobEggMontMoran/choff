import { View, Text, StyleSheet } from "react-native";

/**
 * Placeholder screen for a future Analytics feature
 * This screen is not currently accessible via the main navigation
 * The intention is to develop this into a dashboard that provides users
 * with visualisations of their brewing data and trends
 */
const AnalyticsScreen = () => {
  return (
    <View>
      <Text>Analytics Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default AnalyticsScreen;
