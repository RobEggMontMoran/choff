import { View, Text, StyleSheet } from "react-native";

const BeanEntryScreen = () => {
  return (
    <View>
      <Text>Bean Entry Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default BeanEntryScreen;
